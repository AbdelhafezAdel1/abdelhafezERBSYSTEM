## 🚀 تشغيل سريع

### الأوامر المطلوبة بالترتيب:

```bash
# 1. الدخول لمجلد backend
cd backend

# 2. تثبيت المكتبات
npm install

# 3. تحسين قاعدة البيانات (مرة واحدة فقط)
node optimize_db.js

# 4. تشغيل السيرفر
npm start
```

### بيانات تسجيل الدخول:
- **Username:** admin
- **Password:** 100200300aa

---

## ✅ ما تم إصلاحه:

1. ✔️ **التأخير في تسجيل الدخول** - تم تحسين الاستعلامات وإضافة indexes
2. ✔️ **فشل تسجيل الدخول أحياناً** - تم إصلاح حفظ الـ session
3. ✔️ **تحذير MemoryStore** - تم استخدام PostgreSQL session store

---

## 📌 ملاحظة مهمة:
إذا واجهت أي مشكلة، تأكد من:
- ✅ ملف `.env` يحتوي على بيانات الاتصال الصحيحة
- ✅ قاعدة البيانات PostgreSQL تعمل
- ✅ تم تشغيل `optimize_db.js` مرة واحدة على الأقل
